# Copy to .env and fill in your key from https://aistudio.google.com/apikey
GEMINI_API_KEY=
# Optional — defaults to gemini-2.5-flash
# GEMINI_MODEL=gemini-2.5-flash
# Optional — parallel photo analysis (default 6)
# GEMINI_MAX_WORKERS=6
